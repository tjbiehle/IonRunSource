#pragma strict

function OnGUI () {
	GUILayout.TextArea("Click on the 3D objects to test their click sensors. To try the Exit example you need to Build this scene. To try Load Level, ensure you have added the level you want to load in the Build Settings");
}